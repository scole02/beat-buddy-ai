[project]
name = "fret-and-field"
version = "0.1.0"
description = "A Flask guitar practice room with microphone recording and monophonic transcription"
requires-python = ">=3.11"
dependencies = [
    "defusedxml>=0.7.1",
    "flask>=3.1,<4",
    "gunicorn>=23,<24",
    "numpy>=2.0,<3",
    "openai>=3.14.0",
    "python-dotenv>=1.2.3",
]
